@import url('https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Cormorant:wght@300;600&family=Playfair+Display:wght@500;700&family=Lora&display=swap');

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
    /* Tema Claro */
    :root, [data-theme="flamencoLight"] {
        --color-base-200: #E8D9CD;
        --color-base-300: #DBC6B4;
        --color-base-400: #CEB39B;
        --color-base-500: #C1A082;
        --color-base-600: #A9876B;
        --color-base-700: #8F6E56;
        --color-base-800: #755541;
    }

    /* Tema Oscuro */
    [data-theme="flamencoDark"] {
        --color-base-200: #1e1e1e;
        --color-base-300: #2a2a2a;
        --color-base-400: #363636;
        --color-base-500: #424242;
        --color-base-600: #4e4e4e;
        --color-base-700: #5a5a5a;
        --color-base-800: #666666;
    }
}

.container {
    @apply mx-auto px-4;
}

/* Aplica la fuente automáticamente a todos los elementos */
@layer base {
    html {
        font-family: cormorant; /* Usa 'Lora' para todo el texto */
        @apply text-[18px] md:text-[20px];
    }

    h1, h2, h3, h4, h5, h6 {
        font-family: cinzel; /* Usa 'Cinzel' para títulos */
    }
}
